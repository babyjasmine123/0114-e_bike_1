# 0114-e_bike_1

已经改好背景地图及天气api

存在的问题： 右上角显示的是实时检测结果（可能来自 YOLOv5 的实时检测）。 左下角显示的是跨线时的检测结果；当跨线检测失败（len(bboxes) == 0）时，会被标记为 helmet = 2（"未知"）。

拟定的解决办法： 在跨线检测失败时，使用之前帧的检测结果（需要保存每个 track 的最近检测结果）。 或在检测失败时，尝试使用整张图片进行检测，而不是裁剪后的区域。 或扩大检测区域（添加 padding）。
